# 项目进度

## 状态摘要

| 模块 | 状态 | 说明 |
| --- | --- | --- |
| 基础运行 | 已完成 | `npm start` 启动 Express 服务。 |
| 照片上传 | 已完成 | 支持多图、拖拽、原图优先上传、超大图高质量处理和进度。 |
| 上传整理体验 | 已完成基础版 | 上传时可设置描述模板、事件日期、事件名称、标签和分组。 |
| 前端 UI 体验 | 已完成阶段性优化 | 首页顶部、工具栏、按钮、下拉菜单、主题按钮、故事模式、灯箱编辑页和昵称弹窗已做简约化收敛。 |
| 自定义下拉 | 已完成基础版 | `js/select.js` 增强原生 select，统一上传整理和筛选工具的下拉视觉。 |
| 重复照片检测 | 已完成基础版 | 上传时按内容 hash 跳过重复照片。 |
| 网页系统状态 | 已完成基础版 | 可查看照片数、缩略图数、丢图检查和重复组数量。 |
| 相册浏览 | 已完成 | 网格、时间线、灯箱、懒加载。 |
| 照片整理 | 已完成 | 标签、描述、事件、分组、收藏、搜索、排序。 |
| 批量操作 | 已完成 | 批量删除、批量整理、批量分组。 |
| 评论互动 | 已完成 | 评论、昵称、浏览器本地头像、表情回应。 |
| 主题系统 | 已完成 | 明暗模式、渐变、主题包。 |
| 故事流 | 已完成阶段性优化 | 已有前后端模块；故事列表、普通编辑时间线和全屏回忆海报展示已完成阶段性体验优化，全屏模式包含发光时间线、圆形记忆泡泡、固定背景装饰、可编辑可拖动文案栏、低/中/高三档自动播放速度和上下分布节点。 |
| 缩略图 | 已完成 | sharp 生成和补齐缩略图。 |
| 文档体系 | 已完成初版 | 根目录、`docs/`、主要源码目录和审计输出已建立说明；已新增每轮项目建设必须写入 `docs/project-log.md` 的留痕规范。 |
| 自动化测试 | 已有基础脚本 | 已有 `npm run test:smoke`，仍缺少单元测试和浏览器端测试。 |
| 鉴权 | 已有基础保护 | `/api` 写入接口需要管理密码，但还不是完整账号体系。 |
| 并发保护 | 已有基础保护 | 同进程 API 请求进入队列，多实例部署仍需数据库或分布式锁。 |
| 备份恢复 | 已有备份脚本 | `npm run backup:data` 可本地备份；恢复流程仍需人工执行和演练。 |
| 数据体检 | 已有脚本 | `npm run audit:data` 可检查图片、元数据和故事引用一致性。 |
| 本地启动面板 | 已完成基础版 | `start-local.bat` 可启动网站、检查数据、打开 `uploads/`、查看管理密码。 |

## 当前工作区注意

截至本次文档和加固工作，Git 工作区中已有业务代码未提交改动，包括故事流、写入鉴权、写入队列、脚本和文档等。后续不要回滚这些改动，除非你明确要求。

当前已知上线前注意事项：

- 用户已确认旧相册数据不保留，当前照片元数据已清空，`npm run audit:data` 通过。
- 部分历史界面文案仍建议再做一次 UTF-8 和用户可见文本清理；本轮已将“家庭”定位文案改为更开放的“回忆相册”。
- 生产部署前必须设置强 `DELETE_PASSWORD`，不能使用默认值。
- 浏览器本地头像不随服务器迁移或多设备同步，这是当前轻量实现的有意边界。
## 2026-06-12 进度补充

- 故事流已增强为“普通编辑时间线 + 全屏回忆剧场”两种展示：普通模式保留可拖动节点、上移/下移/归位和叙述编辑；全屏模式提供氛围化曲线时间流、圆形相框、时间标签、描述文本、左右渐隐和总功能按钮。
- 故事外观新增背景能力：相册图片背景和透明度写入 `story-data.json`；电脑本地上传背景只写入当前浏览器 `localStorage`。
- 灯箱编辑页已从过度透明状态恢复为有承托层次的暗色编辑面板，故事模式也恢复了主舞台、时间线、编辑区之间的层次。

## 2026-06-15 进度补充

- 故事全屏展示已升级为参考图方向的二次元回忆海报：发光丝带时间线、串珠节点、圆形记忆泡泡、真实时间/用户文案层级、星空粒子、樱花、胶片、拍立得、纸张和可编辑文案栏均由原生 HTML/CSS/ES Modules 实现。
- 全屏展示打开时会锁定页面纵向滚动，桌面和移动端均通过 Chrome CDP 真实截图检查；移动端保留横向拖动浏览，标题和装饰做了降级收敛。
- 本轮继续修正全屏展示细节：节点文案只取用户填写的照片描述或故事条目备注，缺失时只显示时间；标题下方和底部文案可直接编辑并拖动，内容分别保存到故事 `showcaseSubtitle/showcaseFooter` 字段，位置保存在当前浏览器 `localStorage`。
- 背景粒子、星尘、花瓣和边角装饰已从可拖动时间线层拆出，用户拖动时仅移动时间线和照片节点；hover 改为固定图片底图 + 扩大圆形裁切窗口。
- 全屏自动播放默认速度已放慢，并提供速度滑条；主相册灯箱和故事预览节点都可以编辑单张照片描述、事件日期、事件名和标签。
## 2026-06-15 进度补充

- 故事全屏展示继续向参考图靠拢：图片悬停展开改为更慢的中心外扩过程，时间流线改细，节点光圈和装饰硬边被弱化，整体氛围更柔和。
- 新增 `assets/story-decor/` 本地装饰素材目录，用于存放可追溯的公开装饰素材；当前已用于故事全屏边缘樱花、胶片和纸张装饰，不与 `uploads/` 用户照片目录混用。
- 资源来源和使用方式已整理到 `learning-plan/story-showcase-resources-2026-06-15.md`，包括实际使用资源、备用资源、FreeSVG 下载失败原因和手动下载方法。

## 2026-06-15 进度补充

- 故事全屏展示继续收口细节：照片默认边框变薄，hover 裁切层级修复，放大后形状更不规则；背景滤镜减轻，透明度上限提升到 100%，全屏展示会尊重用户设置的背景透明度。
- 自动播放速度控制改为舒缓的 `30-72px/s` 区间，默认 `40px/s`，滑条数值与实际像素速度一致；全屏边缘装饰已去掉左上角和右下角大花，改用低透明度胶片、纸张、拍立得、星尘、粒子和花瓣等轻细节。

## 2026-06-15 进度补充

- 故事模式普通页的“全屏底部寄语”输入区继续加宽，字数上限从 48 提升到 96，便于填写更完整的寄语。
- 故事全屏展示自动播放速度重新调慢，最低可到 `30px/s`，默认 `40px/s`，最高 `72px/s`，避免最慢档仍过快，并让滑条显示值和真实速度一致。
- 清理故事 `story-mqalexdg-aw4a1z` 中 8 条已断链旧 `Snipaste_...png` 条目，保留当前 5 条存在于 `uploads/` 的故事图片引用并重排 position。
## 2026-06-15 进度补充

- 故事“全屏展示”已恢复为浏览器原生全屏 + 应用内展示层：点击后会占满整个屏幕，也会出现浏览器自带的 Esc 退出提示；自动播放速度范围为 `50-72px/s`，默认 `54px/s`，拖动速度条时会立即重排播放帧并按新速度继续。

## 2026-06-19 进度补充

- 故事全屏展示自动播放速度控制已从精确数值滑条改为“低速 / 中速 / 高速”三档按钮；中速使用 `56px/s` 作为默认体感速度，低速和高速拉开明显差异，播放中切换会立即生效。
