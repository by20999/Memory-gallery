# 项目日志

## 2026-06-11

- 完成：
  - 将项目定位从偏“家庭相册”调整为更开放的“回忆相册”，同步更新 `AGENTS.md`、`README.md`、`docs/project-overview.md`、`docs/api.md`、`docs/data-model.md`、`docs/project-plan.md` 和 `memory/README.md` 中的相关表述与示例。
  - 重构首页顶部为更紧凑的左右分栏，移除顶部 emoji 装饰，降低毛玻璃、卡片感、过重阴影和大块按钮的视觉占比。
  - 统一系统状态、重新检查、打开 uploads、上传、筛选、视图切换等按钮和工具区样式，使页面更偏简约、得体和内容优先。
  - 优化上传整理区文案：精简描述输入占位，替换描述模板为更年轻的表达，事件日期输入使用中文占位并在前端上传时规范化为 `YYYY-MM-DD`。
  - 新增 `js/select.js`，在保留原生 `select` 作为状态源的前提下，提供统一样式的自定义下拉菜单。
  - 优化主题控制按钮，将白天/夜晚和主题按钮从 emoji 改为线性 SVG 图标。
  - 新增浏览器本地自定义头像能力：头像图片在前端裁剪压缩后保存到当前浏览器 `localStorage`，不上传服务器、不改后端数据结构。
  - 优化照片清晰度链路：上传时 100MB 内优先保留原图，超大图片才进行高质量降采样；主画廊卡片、最近上传记忆卡和时间线封面改为原图优先展示，移除不适合照片的 `crisp-edges` 渲染。
  - 继续优化最新上传大图清晰度：取消最新上传卡片的缩放入场动画、照片滤镜和强制 GPU 合成，避免浏览器对刚上传大图做临时降采样；上传响应补充 `uploadTime`，让新插入照片和重新加载后的正式数据一致。
  - 根据真实文件排查确认当前原图为 `1672×941`、缩略图为 `640×360`，将“最近上传/记忆卡”和故事节点这类中大展示位改为原图优先，避免 52KB 缩略图被撑大显示。
  - 将后端缩略图策略从 640px/质量 82 提升到 1600px/质量 92，并在读取照片时检测旧低清缩略图尺寸，低于当前目标时自动重建，降低遗漏展示位误用缩略图导致发糊的风险。
  - 针对页面中占空间最大的最近上传封面，改为直接加载原图，并通过浏览器检查确认 `.group-nav-cover img` 的 `currentSrc` 为 `/uploads/...png?v=...`、自然尺寸为 `1672×941`。
- 决策：
  - 延续原生 HTML/CSS/ES Modules，不引入 shadcn、React 或构建系统。
  - 用户头像属于本地个性化设置，暂不进入服务器数据模型，避免引入账号体系和隐私存储问题。
  - 下拉菜单采用轻量增强层，原生 `select` 仍保留，便于保持既有业务逻辑和可访问状态同步。
  - 将“每轮项目建设后必须留痕”写入协作规范和开发约束。
  - 照片浏览场景优先保证观感清晰：大尺寸展示使用原图，小尺寸封面和预览继续使用缩略图控制加载成本。
- 验证：
  - 多次执行 `node --check` 检查 `js/theme.js`、`js/upload.js`、`js/gallery.js`、`js/select.js`、`js/profile.js`、`js/dom.js`、`js/main.js` 等前端模块。
  - 执行 `node --check js/utils.js`、`node --check js/upload.js`、`node --check js/gallery.js`、`node --check server/routes/photos.js` 验证清晰度相关改动语法。
  - 再次执行 `node --check server/routes/photos.js`、`node --check js/gallery.js`、`node --check js/upload.js`、`node --check js/utils.js` 验证最新上传清晰度修复。
  - 执行 `node --check server/services/thumbnailService.js`、`node --check js/story.js` 验证高质量缩略图与故事节点原图优先改动。
  - 只读检查当前 `uploads/` 和 `/api/photos`，确认当前工作区暂无可用于视觉复测的正式照片；后续需重新上传测试图并强制刷新浏览器验证。
  - 重新上传测试图后，重启服务并用浏览器 CDP 检查实际 DOM：最大封面图已加载原图，`filter` 与 `transform` 均为 `none`。
  - 验证首页可返回 `200`，`/api/photos` 可返回 JSON。
  - 使用 Chrome headless 截图检查桌面和移动端首屏；临时 QA 截图已清理。
- 风险：
  - `style.css` 仍然体积较大，本轮以末尾覆盖层方式收敛视觉，后续仍建议拆分视觉层和业务组件样式。
  - 自定义下拉菜单是轻量实现，后续如需要完整键盘导航、搜索、分组等高级能力，应单独做可访问性增强。
  - 本地头像仅保存在当前浏览器，换设备或清理浏览器数据后不会同步。
  - 主画廊改为原图优先后，超多高分辨率照片同屏时网络和内存压力会高于缩略图方案；当前通过懒加载和并发队列控制。
  - 为保证照片原貌，照片元素不再套统一滤镜调色；不同照片之间的色调一致性会弱一些，但清晰度和真实观感优先。
  - 当前本地上传目录为空，无法继续用刚才那张照片做端到端截图复测；下一次上传后应重点检查最近上传记忆卡、主网格、时间线封面和灯箱四个位置实际加载的资源 URL。
- 后续：
  - 为批量整理替换连续 prompt，改成统一弹窗。
  - 继续拆分 `style.css` 与 `js/gallery.js`。
  - 为前端核心交互补浏览器端冒烟测试。

后续建议按以下格式追加：

```markdown
## YYYY-MM-DD

- 完成：
- 决策：
- 风险：
- 后续：
```
